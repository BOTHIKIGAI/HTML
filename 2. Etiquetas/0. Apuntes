<!-- HTML
Etiqueta "container" para todo el documento HTML. Todas las demás etiquetas
están escritas en el elemento HTML.
-->
<html></html>

<!-- Head
Contenedor de encabezado. Por lo general, el encabezado incluye referencias
a otros archivos que necesita la página o el sitio. También puede incluir 
datos que los motores de búsqueda y los sitios de redes sociales usan para 
que sea más sencillo encontrar su sitio web.
-->
<head></head>

<!-- Body
Esta etiqueta crea el contenedor body para la página web. La mayoría de los
elementos de visualización van en el elemento body.
-->
<body> </body>

<!-- Div
La etiqueta de división crea una sección en el HTML. Por lo general, las 
secciones tienen contenido similar o contenidos que se relacionan entre sí.
-->
<div></div>

<!-- Negrita -->
<strong>Hola Mundo</strong>

<!--Encabezados
Exsiten 6 tipos de encabezados, el primero es el mas grande y el ultimo el menos grande
-->
<h1>Encabezado 1</h1>
<h2>Encabezado 2</h2>
<h3>Encabezado 3</h3>
<h4>Encabezado 4</h4>
<h5>Encabezado 5</h5>
<h6>Encabezado 6</h6>

<!-- Salto de linea    
El elemento salto de linea break <br> produce un salto del linea en el texto
(retorno de carro). Es util para escribir un pema o una direccion, donde la 
division de las lineas es significante.
-->

<br>

<!-- Unordered list
ul de "unordered list" -lista no ordenada . crea una lista no ordenada.: 
Sus etiquetas son: <ul> y </ul> (ambas obligatorias). Está definido como: 
Elemento para listas. Crea una caja: En bloque. Puede contener: Uno o más 
elementos li
-->

<ul>
    <li>punto uno</li>
    <li>punto dos</li>
    <li>punto tres</li>
</ul>

<!-- Ordenend list
El elemento ol permite definir listas o viñetas ordenadas (“Ordered List”),
bien con numeración o alfabéticamente.
Sus etiquetas son: <ol> y </ol> (ambas obligatorias).

-->

<ol>
    <li>punto uno</li>
    <li>punto dos</li>
    <li>punto tres</li>
</ol>

<!-- Lista 
La etiqueta <li> en HTML se utiliza para definir el elemento de la lista 
en un documento HTML. Se usa dentro de una Lista Ordenada <ol> o Lista No 
Ordenada <ul>
    
-->

<li></li>

<!-- Hipervinculos o elemento ancla
El Elemento HTML Anchor <a> crea un enlace a otras páginas de internet, 
archivos o ubicaciones dentro de la misma página, direcciones de correo, 
o cualquier otra URL

El recurso al que se hace referencia se indica con un atributo denominado 
href (o referencia de hipertexto). Los atributos se agregan dentro de la 
etiqueta de apertura y suelen ser un nombre emparejado con un valor (lo que
 se denomina "pares clave-valor"), con el valor incluido entre comillas.

-->

<a href=""></a>

<!-- Imagenes o Elemento incrustado de imagen
El elemento de imagen HTML <img> representa una imagen en el documento.

El atributo alt dentro de img define el texto alternativo que describe la
imagen, texto que los usuarios verán si la URL de la imagen es errónea o 
la imagen tiene un formato no soportado o si la imagen aún no se ha 
descargado.

El atributo src se usa para apuntar a la ubicación de la imagen que se va a
mostrar en la página
-->

<img src="" alt="">

